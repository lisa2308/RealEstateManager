<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/activity_main_menu_add_estate"
        android:icon="@drawable/ic_plus"
        android:title="Add estate"
        app:showAsAction="always"/>

    <item android:id="@+id/activity_main_menu_search"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:showAsAction="always" />

    <item android:id="@+id/activity_main_menu_loan_simulator"
        android:icon="@drawable/ic_calculator"
        android:title="Loan simulator"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/activity_main_menu_map"
        android:icon="@drawable/ic_map"
        android:title="Map"
        app:showAsAction="ifRoom" />
</menu>